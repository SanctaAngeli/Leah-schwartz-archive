[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: every route is served by index.html and resolved
# client-side by React Router (deep links like /her-words/immigrants).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
